REACT_APP_GOOGLE_CLIENT_ID=
REACT_APP_API_BASE_URL=http://localhost:4000

# DEV ONLY — shows a bypass login form instead of requiring Google sign-in.
# Set this in a gitignored .env.local, never here. Leave false in any committed file.
REACT_APP_ENABLE_DEV_LOGIN=false
